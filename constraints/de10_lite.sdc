# =============================================================================
# DE10-Lite SDC Timing Constraints
# =============================================================================

# ── System clock constraint ─────────────────────────────────────────────────
# Board oscillator is 50 MHz (20 ns).
create_clock -name clk_50m -period 20.000 [get_ports MAX10_CLK1_50]

# 25 MHz system clock (toggle FF divider in top_fpga)
create_generated_clock -name clk_25m \
    -source [get_ports MAX10_CLK1_50] \
    -divide_by 2 \
    [get_registers {clk_25m}]

# ── Input / output delay (relaxed — buttons, VGA DAC) ──────────────────────
set_input_delay  -clock clk_25m -max 5.0 [get_ports {KEY[*]}]
set_input_delay  -clock clk_25m -min 0.0 [get_ports {KEY[*]}]

set_output_delay -clock clk_25m -max 2.0 [get_ports {VGA_R[*] VGA_G[*] VGA_B[*] VGA_HS VGA_VS}]
set_output_delay -clock clk_25m -min 0.0 [get_ports {VGA_R[*] VGA_G[*] VGA_B[*] VGA_HS VGA_VS}]

set_output_delay -clock clk_25m -max 2.0 [get_ports {LEDR[*] GPIO[*]}]
set_output_delay -clock clk_25m -min 0.0 [get_ports {LEDR[*] GPIO[*]}]

# ── Clock uncertainty (removes Critical Warning 332168) ─────────────────────
derive_clock_uncertainty

# ── SDRAM I/O timing ───────────────────────────────────────────────────────
# DRAM_CLK is generated by inverting MAX10_CLK1_50 (180° phase shift).
# Create a virtual clock representing the SDRAM-side sampling edge.
create_generated_clock -name sdram_clk \
    -source [get_registers {clk_25m}] \
    -invert \
    [get_ports DRAM_CLK]

# Output constraints: FPGA→SDRAM (data, address, commands)
# IS42S16320D-7TL :  tDS(min)=1.5ns  tDH(min)=0.8ns
set_output_delay -clock sdram_clk -max  1.5 [get_ports {DRAM_DQ[*] DRAM_ADDR[*] DRAM_BA[*] DRAM_CAS_N DRAM_RAS_N DRAM_WE_N DRAM_CS_N DRAM_CKE DRAM_LDQM DRAM_UDQM}]
set_output_delay -clock sdram_clk -min -0.8 [get_ports {DRAM_DQ[*] DRAM_ADDR[*] DRAM_BA[*] DRAM_CAS_N DRAM_RAS_N DRAM_WE_N DRAM_CS_N DRAM_CKE DRAM_LDQM DRAM_UDQM}]

# Input constraints: SDRAM→FPGA (read data)
# IS42S16320D-7TL :  tAC(max)=5.4ns  tOH(min)=2.5ns  (CL=2)
set_input_delay -clock sdram_clk -max  5.4 [get_ports {DRAM_DQ[*]}]
set_input_delay -clock sdram_clk -min  2.5 [get_ports {DRAM_DQ[*]}]

# ── False paths (async inputs — buttons) ────────────────────────────────────
set_false_path -from [get_ports {KEY[*]}]
